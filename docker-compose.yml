services:
  mongo:
    image: mongo:7
    container_name: afya-mongo
    ports:
      - "27017:27017"
    volumes:
      - afya-mongo-data:/data/db
    restart: unless-stopped

volumes:
  afya-mongo-data:
